# CleanupTemp Pro — Changelog / Список изменений

---

## 🛠 Исправления багов

### 1. Счётчики статистики суммировались при повторном сканировании
**Было:** После очистки и повторного нажатия «Сканировать» счётчики в панели статистики
(Temp-файлы, Кэш браузеров, Корзина) не сбрасывались — новые результаты суммировались
со старыми, показывая завышенные цифры.

**Стало:** После завершения очистки все внутренние счётчики и UI-метки сбрасываются
в ноль. Каждое новое сканирование начинается с чистого листа.

---

### 2. Кнопка «Сканировать» оставляла тень при уходе мыши
**Было:** При наведении на кнопку появлялся glow-эффект, но при уходе курсора тень
не убиралась полностью — оставалась приглушённая подсветка, в отличие от кнопок
«Очистить» и «Стоп», которые работали корректно.

**Стало:** Поведение унифицировано — тень полностью убирается при уходе мыши,
все три кнопки ведут себя одинаково.

---

### 3. Неточное форматирование размера в счётчике «Очищено»
**Было:** В процессе удаления счётчик очищенного места умел показывать только МБ
или ГБ. Если удалено меньше 1 МБ — отображалось `0,0 МБ` вместо реального значения
в КБ. Логика дублировалась в двух местах (файлы + корзина).

**Стало:** Оба места используют единый метод `SizeHelper.Format()`, который корректно
отображает Б / КБ / МБ / ГБ в зависимости от фактического размера.

---

## ✨ Новое: предупреждение об открытых браузерах

**Добавлено:** Перед запуском очистки программа проверяет, запущены ли браузеры
(Chrome, Edge, Firefox, Brave, Opera, Opera GX, Яндекс Браузер, Vivaldi).

Если браузер открыт и включён пункт «Кэш браузеров» — появляется диалоговое окно
с предупреждением и объяснением, **почему после очистки снова находится мусор**:
браузер работает и немедленно воссоздаёт файлы кэша.

Пользователь может закрыть браузеры и повторить очистку — или продолжить несмотря
на предупреждение.

---

## 💡 Новое: подсказка в строке статуса после сканирования

**Добавлено:** Если после сканирования найден мусор и при этом браузеры открыты —
в строке статуса появляется метка `⚠ закройте браузеры перед очисткой`.

Это помогает пользователю сразу понять причину, не гадая почему цифра не меняется
после нескольких очисток подряд.

---

## 📋 Затронутые файлы

| Файл | Изменения |
|------|-----------|
| `MainWindow.xaml.cs` | Все исправления и новые функции |

Остальные файлы проекта (`App.xaml`, `CustomDialog`, `AboutWindow`, `.csproj`) —
**не затронуты**.
