# CleanupTemp Pro — Changelog / Список изменений

---

## 🛠 Исправления багов

### 1. Файлы WebCacheV01.dat, WebCacheV01.jfm (живая БД Edge) больше не показываются — папка WebCache исключена из сканирования полностью

Файлы journal.baj, journal.log, index (кэш Chromium) добавлены в белый список — браузер пересоздаёт их мгновенно
.dll, .exe, .pdb, .xml, .json и другие рабочие расширения в Temp больше не считаются мусором
Папки .net, VBCSCompiler, VSLogs, MSBuild, Cloudflare WARP, nvidia, AMD в Temp защищены — файлы из них не трогаются
desktop.ini в $RECYCLE.BIN и везде больше не показывается и не удаляется
.tmp файлы заблокированные активными процессами пропускаются
Файлы 0 байт в Temp пропускаются
Фильтр по возрасту: Temp — минимум 5 минут, браузерный кэш — 2 минуты, WU кэш и мусор в корне диска — 5 минут

---

### 2. Баги UI

Статус "Система чиста!" теперь сбрасывается мгновенно при старте нового сканирования
Прогресс-бар показывает "Система чиста! ✓" вместо "Сканирование... 100%" по завершении
Двойной клик на "Очистить всё" больше не запускает очистку дважды
Кнопка "Очистить" теперь активна при повторном нажатии "Сканировать"
Прогресс не прыгает хаотично между категориями при параллельном сканировании

---

### 3. Баги кода

Гонка на Stopwatch в параллельном цикле удаления — заменено на Interlocked
Неверный счётчик прогресса при удалении (сравнение с regular вместо regularFiles)
Небезопасный порядок Cancel/Dispose у CancellationTokenSource
return внутри finally убран (ошибка компилятора)
Утечка CancellationTokenSource в ScanDir — исправлено через finally

---

## ✨ Новое

Полностью переработана логика фильтрации (защищённые папки, расширения, возраст файлов)
Исправлены race condition баги в многопоточном коде
Переработан UI/UX (статусы, прогресс, кнопки)
Параллельное сканирование

---